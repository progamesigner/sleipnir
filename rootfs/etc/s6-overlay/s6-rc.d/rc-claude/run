#!/command/with-contenv sh


[ "${SLEIPNIR_ENABLE_RC_CLAUDE:-0}" = "1" ] || { s6-svc -O . ; exit 0 ; }

cd /workspace || exit 1
exec s6-setuidgid ubuntu claude remote-control \
    --name "${CLAUDE_RC_NAME:-${TS_HOSTNAME:-Sleipnir}}" \
    --spawn same-dir
